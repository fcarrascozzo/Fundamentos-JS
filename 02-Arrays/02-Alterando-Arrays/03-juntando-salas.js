/* 
Haverá uma palestra sobre padrões de projetos para os alunos das salas de 
Javascript e Python

Junte ambas as salas em uma única lista que exiba todas as alunas. 
*/

const salaJS = ["Fernando", "André", "Karine"];
const salaPY = ["Carlos", "Hadrian", "Tainara"];

// concat concatena duas listas

const salaUnica = salaJS.concat(salaPY);

console.log(salaUnica);